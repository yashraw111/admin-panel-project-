<!DOCTYPE html>
<html dir="ltr" lang="en">
  <%- include('../layout/headSection') %>
  <body>
    <div class="preloader">
      <div class="lds-ripple">
        <div class="lds-pos"></div>
        <div class="lds-pos"></div>
      </div>
    </div>
    <div id="main-wrapper">
<%- include('../layout/header') %>
<%- include('../layout/sidebar') %>
<% console.log(subCategory) %>
<% console.log(Category) %>
      <div class="page-wrapper">
        <div class="page-breadcrumb">
        </div>
        <form class="form-horizontal" method="post" action="/api/subCategory/edit/<%= subCategory._id %>">
          <div class="card-body">
              <h4 class="card-title">update Category</h4>
              <div class="form-group ">
                <select name="cat_name" class="form-control" >
                  <option value="">--Category--</option>
                  <% Category.forEach(cat => { %>
                    <option value="<%= cat._id %>" <%= cat.cat_name ===subCategory.cat_name.cat_name ? "selected" :""  %>><%= cat.cat_name %></option>
                  <%    }) %>
                </select>
              </div>
              <div class="form-group ">
                  <div class="col-sm-9 mt-3">
                      <input type="text" class="form-control" value="<%= subCategory.sub_category %>" id="fname" placeholder="Enter  subCategory Name " name="sub_category">
                  </div>
              </div>
             
          </div>
              <div class="card-body">
                  <button class="btn btn-github">Submit</button>
              </div>
      </form>
        <footer class="footer text-center">
          All Rights Reserved by Matrix-admin. Designed and Developed by
          <a href="https://wrappixel.com">WrapPixel</a>.
        </footer>
      </div>
    </div>

    <%- include('../layout/jsLink') %>
  </body>
</html>
  